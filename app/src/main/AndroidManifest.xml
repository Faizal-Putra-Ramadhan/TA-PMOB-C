<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AdaKonser"
        tools:targetApi="31">

        <!-- Login Activity (Launcher) -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Register Activity -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false" />

        <!-- Main Activity (User Home) -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- User Activities -->
        <activity
            android:name=".BuyTicketActivity"
            android:exported="false" />

        <activity
            android:name=".MyTicketsActivity"
            android:exported="false" />

        <activity
            android:name=".TicketDetailActivity"
            android:exported="false" />

        <activity
            android:name=".PromoListActivity"
            android:exported="false" />

        <!-- Admin Activities -->
        <activity
            android:name=".AdminDashboardActivity"
            android:exported="false" />

        <activity
            android:name=".AdminEventManagementActivity"
            android:exported="false" />

        <activity
            android:name=".AdminAddEventActivity"
            android:exported="false" />

        <activity
            android:name=".AdminManageTicketsActivity"
            android:exported="false" />

        <activity
            android:name=".AdminPromoManagementActivity"
            android:exported="false" />

    </application>

</manifest>